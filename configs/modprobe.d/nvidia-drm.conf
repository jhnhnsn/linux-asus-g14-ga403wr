# /etc/modprobe.d/nvidia-drm.conf
# Hybrid AMD iGPU + NVIDIA dGPU: enable KMS render offload, but do NOT let
# nvidia take the framebuffer/console (that black-screens the amdgpu panel).
options nvidia_drm modeset=1 fbdev=0
